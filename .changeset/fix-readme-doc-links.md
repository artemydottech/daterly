---
"@artemy-tech/rtdp": patch
---

- Выбранный день в календаре больше не «раздувается»: убраны дефолтные `font-weight: bold` и `font-size: large` от react-day-picker, кружок selected сделан меньше cell — больше не упирается в соседние числа.
- Обновлены ссылки в README на актуальные пути документации (`/docs/recipes`, `/docs/theming`, `/docs`).
