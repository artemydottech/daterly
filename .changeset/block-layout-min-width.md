---
"@artemy-tech/datepicker": minor
---

DatePicker и DateRangePicker теперь растягиваются на 100% контейнера (`display: block`) — как в antd. Ширина контролируется обёрткой.

Добавлены CSS-токены `--daterly-field-min-width` (220px) и `--daterly-range-min-width` (280px) для переопределения минимальной ширины без `!important`.
