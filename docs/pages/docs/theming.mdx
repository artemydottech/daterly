import ThemePresets from '../../components/ThemePresets'

# Стилизация

Вся визуальная настройка — через CSS-переменные с префиксом `--rtdp-*`. Никакого CSS-in-JS, никаких className-инъекций.

## Готовые пресеты

Кликни по пресету слева — справа сразу видно превью и CSS-токены, которые нужно скопировать в свой `:root`.

<ThemePresets />

## Подключение

```tsx
import '@artemy-tech/rtdp/styles'
```

Этот импорт подгружает базовые переменные и стили `react-day-picker`. После этого переопределяй нужные токены через `:root` или scope-селектор.

## Основные токены

```css
:root {
  /* Цвета */
  --rtdp-color-accent: #7c83ff;
  --rtdp-color-text: #1a1a1a;
  --rtdp-color-placeholder: #9e9e9e;
  --rtdp-bg: #ffffff;

  /* Рамки */
  --rtdp-border-color: #e0e0e0;
  --rtdp-border-color-focus: var(--rtdp-color-accent);
  --rtdp-border-color-failed: #ef4444;
  --rtdp-radius: 8px;

  /* Типографика */
  --rtdp-font-family: inherit;
  --rtdp-font-size: 14px;

  /* Календарь */
  --rtdp-day-bg-selected: var(--rtdp-color-accent);
  --rtdp-day-bg-hover: rgba(124, 131, 255, 0.1);
  --rtdp-day-bg-range: rgba(124, 131, 255, 0.15);
}
```

## Состояния через `data-*`

Состояния не задаются классами — они выражены через `data-*` атрибуты на корневом элементе:

```css
.rtdp[data-focused]  { /* в фокусе или попап открыт */ }
.rtdp[data-filled]   { /* в инпуте есть значение */ }
.rtdp[data-failed]   { /* состояние ошибки */ }
.rtdp[data-disabled] { /* disabled или loading */ }
```

Это позволяет стилизовать состояния без JS-классов и без `aria-*` костылей.

## Скоуп

Если нужны разные темы для разных частей приложения — задавай переменные на конкретном предке:

```css
.dark-section {
  --rtdp-bg: #1a1a1a;
  --rtdp-color-text: #e0e0e0;
  --rtdp-border-color: #333;
}
```

## Тёмная тема

Готовой `[data-theme="dark"]` темы в пакете нет — задаётся пользователем через переопределение переменных. Минимальный пример:

```css
@media (prefers-color-scheme: dark) {
  :root {
    --rtdp-bg: #0a0a0f;
    --rtdp-color-text: #e6e7ee;
    --rtdp-color-placeholder: #6a6e80;
    --rtdp-border-color: rgba(255, 255, 255, 0.1);
  }
}
```
